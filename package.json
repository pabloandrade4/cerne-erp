{
  "name": "cerne-erp-server",
  "version": "0.1.0",
  "private": true,
  "description": "Backend do ERP Cerne — API + servidor do front-end.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node db/migrate.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "dotenv": "^16.4.5"
  }
}
